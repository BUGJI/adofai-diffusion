# ADOFAI Diffusion — GPU 版依赖（与开发机 venv 实测版本一致）
# CPU 用户请用 requirements-cpu.txt。
# torch 默认从 PyPI 装（自带 CUDA 12.8 运行库）；如需换源/换 CUDA 版本见 README。

# —— 数值 / 音频 ——
numpy==2.4.6
scipy==1.18.0
soundfile==0.14.0
librosa==0.11.0

# —— 深度学习 ——
torch==2.11.0
torchaudio==2.11.0

# —— 人声分离 & 拍点跟踪（权重均随包/随仓库分发，不需联网下载）——
demucs==4.1.0
beat-this==1.1.0

# —— 可视化 / 杂项 ——
matplotlib==3.11.1
tqdm==4.70.0

# —— 仅 Windows：GUI（gui/gui_main.py 通过 COM 承载 WebView2）——
comtypes==1.4.16; sys_platform == 'win32'
