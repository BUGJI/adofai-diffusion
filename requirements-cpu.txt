# ADOFAI Diffusion — CPU 版依赖（无 N 卡 / Docker 容器内用这个）
# 注意：装 torch CPU 版必须带 --index-url，普通 pip install -r 会装成 GPU 版：
#   pip install -r requirements-cpu.txt --extra-index-url https://download.pytorch.org/whl/cpu
# 国内网络可在后面再追加清华镜像源。

# —— 数值 / 音频 ——
numpy==2.4.6
scipy==1.18.0
soundfile==0.14.0
librosa==0.11.0

# —— 深度学习（CPU 构建）——
torch==2.11.0+cpu
torchaudio==2.11.0+cpu

# —— 人声分离 & 拍点跟踪 ——
demucs==4.1.0
beat-this==1.1.0

# —— 可视化 / 杂项 ——
matplotlib==3.11.1
tqdm==4.70.0

# —— 仅 Windows：GUI ——
comtypes==1.4.16; sys_platform == 'win32'
